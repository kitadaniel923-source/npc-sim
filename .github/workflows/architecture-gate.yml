name: Everglen Architecture Gate

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ownership-gate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run architecture gate
        run: bash architecture_gate.sh
